# 这是 QQ for Linux 二进制的占位文件，不是真实二进制。
# 真实构建时，请把官方 QQ for Linux 的完整安装目录（含 qq 二进制、resources/、
# chrome-sandbox 等）放到构建上下文的 ./qq-linux/ 下，并将本文件替换为该目录，
# 或修改 Dockerfile 中对应的 COPY 来源。
# 受版权与体积(约204MB)限制，QQ 二进制不能进入 git 仓库。
# 若不想自行构建，请改用 README 的「方案B：docker save/load 现有镜像」。
