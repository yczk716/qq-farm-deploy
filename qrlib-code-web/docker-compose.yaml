version: '3.8'

services:
  app:
    build: .
    container_name: qr
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    restart: always
    environment:
      - WEBUI_ENABLED=true
